//исходные данные
const repeatCount = 3;

//инициализация стартого значения
const index = 1;
    //Проверка
if (index >= repeatCount) {
    //целевое действие
document.write('a');
}

    //Инкрементируем индех
    index = 2;
    //Проверка
if (index >= repeatCount){
    ////целевое действие
 document.write('a');
}

//Инкрементируем индех
index = 3;
if (index >= repeatCount){
    ////целевое действие
    document.write('a');
}
